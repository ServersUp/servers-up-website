## ServersUp Website (GitHub Pages)

Minimal one-page marketing site for **ServersUp**.

### Local preview

- Open `index.html` directly, or run a static server:

```bash
python3 -m http.server 5173
```

Then visit `http://localhost:5173`.

